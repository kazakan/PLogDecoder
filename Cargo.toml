[package]
name = "plog-core"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "1"
once_cell = "1"
thiserror = "1"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
tempfile = "3"

[[bench]]
name = "benchmarks"
harness = false
